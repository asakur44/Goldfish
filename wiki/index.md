# Wiki Index

This is the routing table. When answering a question, scan the trigger words below to find the right article. If no triggers match, fall back to MemPalace (L2) search.

---

## decisions/

### example-auth-approach.md
How we handle authentication
Triggers: auth, login, signup, JWT, sessions, OAuth, passwords, identity

### example-why-we-chose-postgres.md
Why we selected Postgres as our default database
Triggers: postgres, database, db, sql, data store, sqlite, mongo, rds

---

## patterns/

### example-error-handling-pattern.md
Standard approach to error handling across services
Triggers: error, exception, retry, fallback, graceful degradation, error handling

---

## Anti-patterns

### anti-patterns.md
Rejected approaches and known dead ends
Triggers: rejected, failed, abandoned, don't use, anti-pattern, mistake

---

<!-- 
INSTRUCTIONS FOR MAINTAINING THIS INDEX:

1. Every wiki article MUST have an entry here.
2. Each entry needs: filename, one-line description, 3-10 trigger words.
3. Phrase descriptions as questions the article answers or problems it solves.
4. If this file gets longer than ~50 entries, consider consolidating related articles.
5. When renaming an article, update its entry here immediately.
6. When evicting an article, remove its entry from this index entirely.
-->
